spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/jobbridge_user_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9080/realms/test
keycloak:
  realm: test
  endpoint: http://localhost:9080
  application:
    client-id: test
    client-secret: AVk9ezqmsWgMetklI0zXsDASuh3gwE0j
  admin-username: admin
  admin-password: admin
server:
  port: 8081